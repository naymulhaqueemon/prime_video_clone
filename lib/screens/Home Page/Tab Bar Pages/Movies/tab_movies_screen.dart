import 'package:flutter/material.dart';

class TabMoviesScreen extends StatelessWidget {
  const TabMoviesScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}