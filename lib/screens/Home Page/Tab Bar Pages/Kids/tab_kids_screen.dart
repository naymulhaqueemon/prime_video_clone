import 'package:flutter/material.dart';

class TabKidsScreen extends StatelessWidget {
  const TabKidsScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}